/**
 * Node half of the pixel skin. Deliberately empty: the host has no behavior;
 * the browser half mounts through the `dsh.client` web plugin table and only
 * paints the UI, so no host service is provided or consumed.
 */
/** Host plugin body. */
export function apply() { }
